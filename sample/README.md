# Sample for RestX application

Quite simple to use:

- Create a restx with `restx deps install + app compile + app archive /tmp/restx-app.jar`
- Copy the restx jar in the same directory that the `Dockerfile`
- Build the image
- And *voilà*
